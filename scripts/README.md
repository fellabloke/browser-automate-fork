# Repository scripts

- `check.sh` is the canonical deterministic validation command.
- `maintenance/` contains local state-maintenance utilities.
- `diagnostics/` contains import, platform, and packaging diagnostics.
- `smoke/` contains opt-in browser/provider scripts; these are not pytest tests
  and may require live services, credentials, or manual observation.
