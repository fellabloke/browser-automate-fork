#!/bin/bash
cd "/home/sandeep/agent first IDE"
.venv/bin/python run_v16.py run "Navigate to Google AI Studio (https://aistudio.google.com/app/apikey). Check if you are logged in. If presented with a login screen, click 'Sign In' or 'Continue with Google' and click on the existing Gmail account to login. Once on the API Keys dashboard, click 'Create API key' (and 'Create API key in new project' if prompted). If it asks for a name, enter 'Sandeep'. Finally, copy the newly generated API key string, ensure it is visible, and mark the task as done."
