#!/bin/bash
cd "/home/sandeep/agent first IDE"
.venv/bin/python run_v16.py run "Navigate to Cerebras Inference API Console (try https://inference.cerebras.ai or search for it if that fails). Check if you are logged in. If you see a login screen, click 'Continue with Google' or 'Sign In' and select the existing Gmail account. Once logged in, navigate to the API Keys section. Click 'Create API Key'. If it asks for a name, enter 'Akash'. Finally, click to create, copy the generated API key, and mark the task as complete."
